---
interface Props {
  title: string;
  class?: string;
}

const { title, class: className } = Astro.props;
---

<section>
  <div class="bg-green-600/50 px-3 sticky top-0">
    <h3 class="font-bold">{title}</h3>
  </div>
  <div class={className}>
    <slot />
  </div>
</section>
